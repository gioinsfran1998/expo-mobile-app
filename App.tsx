import { StatusBar } from 'expo-status-bar';
import { SafeAreaView, StyleSheet, Text, View } from 'react-native';
import Start from './src/pages/App/Start';

export default function App() {
  return <Start />;
}
